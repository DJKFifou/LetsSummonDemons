{
  "dependencies": {
    "@lsd/common": "^0.0.0",
    "jest": "^29",
    "lodash": "^4",
    "nodemon": "^3.0.1",
    "socket.io": "^4"
  },
  "devDependencies": {
    "@faker-js/faker": "^8"
  },
  "name": "@lsd/back",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -w -p tsconfig.json",
    "dev": "npm run build:watch & nodemon build/main.js",
    "start": "node build/main.js",
    "test": "jest --watch",
    "test:ci": "jest"
  },
  "type": "module",
  "version": "0.0.0"
}
