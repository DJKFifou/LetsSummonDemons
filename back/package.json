{
  "dependencies": {
    "@lsd/common": "^0.0.0",
    "jest": "^29.7.0",
    "lodash": "^4.17.21",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.2.0"
  },
  "name": "@lsd/back",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -w -p tsconfig.json",
    "lint": "eslint . --ext .ts --ext .js --ext .mjs --ext .mts --ext tsx --ext jsx",
    "prebuild": "npm run lint",
    "prettier": "prettier --config .prettierrc --write .",
    "start": "npm run build:watch & npm run start:build",
    "start:build": "node build/src/main.js",
    "test": "jest --watch",
    "test:ci": "jest"
  },
  "type": "module",
  "version": "0.0.0"
}
